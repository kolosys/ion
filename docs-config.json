{
  "packages": [
    {
      "name": "workerpool",
      "description": "Bounded worker pool with context-aware submission and graceful shutdown",
      "priority": 1
    },
    {
      "name": "ratelimit",
      "description": "Token bucket and leaky bucket rate limiters with configurable options",
      "priority": 2
    },
    {
      "name": "semaphore",
      "description": "Weighted semaphore with configurable fairness modes",
      "priority": 3
    }
  ],
  "docs": {
    "root_dir": ".",
    "docs_dir": "docs",
    "templates_dir": "docs-templates"
  },
  "discovery": {
    "enabled": true,
    "exclude_patterns": [
      "*_test.go",
      "examples/*",
      "scripts/*",
      ".scripts/*"
    ],
    "include_only_with_godoc": true
  },
  "output": {
    "generate_combined_api": true,
    "generate_examples": true,
    "verbose": true
  }
}
